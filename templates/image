{{define "bodystuff"}}

        <div id="content" style="font-size:20px; height:500px">
                <div>
                        <img src="data:image/jpeg;base64,{{.Image}}" style="width:30%;"/> 
                </div>
        </div>

{{end}}
